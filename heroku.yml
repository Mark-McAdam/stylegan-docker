build:
  docker:
    # web: Dockerfile
    web: ./backend/Dockerfile.web
    frontend: ./frontend/Dockerfile.web
# run:
#   backend: pip install -r requirements.txt
#   frontend: pip install -r requirements.txt